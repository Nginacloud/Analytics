Create- get retrieves data from the server
Read- post writes new data to the server
Delete -delete removes stuff
Update -put updates data/idempotent
Patch

Stack overflow;
api.stackexchange.com
postman
get:
import requests
import json
reponse = requests.get('url')
print(response.json()['items'])
for data in response.json()['items']:
print(data['title'])

FLASK_APP="api.py:app"
FLASK_ENV="development"